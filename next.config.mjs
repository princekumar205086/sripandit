const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'randomuser.me',
            },
            {
                hostname: ['www.okpuja.in', 'www.sripandit.com'],
            },
        ],
    },
};

export default nextConfig;